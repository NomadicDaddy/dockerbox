name: Validate

on:
    push:
    pull_request:

jobs:
    shell-validation:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Lint and Format
              run: bash lint.sh
